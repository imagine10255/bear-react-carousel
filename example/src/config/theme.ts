export interface AppTheme {
    primaryColor: string,
    dangerColor: string,
    sliderZIndex: number,
    navbarZIndex: number,
}

const theme: AppTheme = {
    navbarZIndex: 14,
    sliderZIndex: 12,
    primaryColor: '#09d3ac',
    dangerColor: '#EF4444',
};

export default theme;
